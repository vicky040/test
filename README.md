install dependencies by -->  npm i
start frontend by --> npm run dev
start backend by --> npm start
to build .dist folder of react --> npm run build

replace all the local host backend url after deploy 
replace all the api key in .env file in backend folder
replace all the BaseUrl variable accross the frontend project with the web address of the hosted backend 
replace the frontend url in app.js in backend folder with actual website address for CORS policy 

sign in to cloudinary and paste its keys to backend .env file